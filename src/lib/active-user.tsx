// This file is intentionally empty.
//
// The placeholder "Acting as" active-user mechanism has been replaced by real
// JWT authentication via src/lib/auth.tsx (useAuth hook + AuthProvider).
//
// If you see a TypeScript import error pointing here, the importing file still
// uses the old useActiveUser() — replace it with useAuth() from @/lib/auth.
